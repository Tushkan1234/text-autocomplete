model:
  embed_dim: 64
  hidden_dim: 128
  num_layers: 2

training:
  batch_size: 256
  learning_rate: 0.001
  num_epochs: 5
  max_rows: 50000

data:
  max_len: 20
  train_size: 0.8
  val_size: 0.1
  test_size: 0.1